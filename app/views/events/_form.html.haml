= simple_form_for(@event, html: { class: 'form-horizontal' }) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :description
    -# = f.input :data

  .form-actions
    = f.button :submit
